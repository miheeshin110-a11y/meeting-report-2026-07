# 경영회의자료 검토 보고서 작업 지침

이 저장소에서 `kpi-*-review.html` 보고서를 작성하거나 수정할 때 아래 규칙을 항상 적용한다.

자료 제출 대상은 다음 5개 팀으로 고정한다.

1. 제품팀
2. 미디어커머스
3. 이커머스
4. 글로벌
5. 유통사업

## 필수 검토 자료

Google Drive 기준 폴더는 `.codex/kpi-report-source.local.md`에서 확인한다. 이 로컬 설정 파일과 Drive URL·폴더 ID는 공개 저장소에 커밋하지 않는다.

보고서를 작성하기 전에 다음 자료를 모두 확인한다.

1. Drive 폴더의 최신 종합 KPI 회의자료 전체
2. 최신 회의자료와 연결되는 최근 회의록 전체
3. 직전 회차 종합 KPI 회의자료와 회의록
4. 이 저장소의 과거 KPI 검토 보고서 전체
5. 최소 비교 기준인 `kpi-260902-review.html`과 `kpi-260907-review.html`
6. `.codex/kpi-report-source.local.md`에 등록된 Google Docs `경영회의 해석 오류` 최신본

## 담당자 해석 정정 확인

`경영회의 해석 오류` 문서는 담당자가 직접 확인한 해석 정정 근거다. 새 보고서를 작성할 때마다 문서의 최신 revision을 다시 읽고, 현재 회차 및 해당 팀에 관련된 내용을 확인한다.

- 담당자가 `해석 오류`라고 명시한 항목은 기존 보고서나 AI의 추론보다 우선한다.
- 정정된 항목을 새 보고서에서 다시 수치 오류·미이행·리스크로 지적하지 않는다.
- 필요한 경우 보고서에는 `담당자 확인에 따라 해석 정정`으로 표시하고, 올바른 해석과 적용 범위를 적는다.
- 팀 제목만 있고 정정 내용이 비어 있으면 해당 팀에 등록된 해석 정정이 없는 것으로 처리한다.
- 과거 회차 정정을 현재 회차에 자동 적용하지 않는다. 회의일·자료명·팀·페이지·지표 등 적용 대상을 대조한다.
- 정정 내용과 최신 원자료가 다시 충돌하면 오류로 단정하지 않고 `담당자 재확인 필요`로 분리한다.
- 별도 요청이 없는 한 `경영회의 해석 오류` 문서 자체는 수정하지 않는다.

파일명만으로 회의일이나 최신본을 단정하지 않는다. 문서 내부의 진행일, Drive 수정 시각, 자료 간 연결 관계를 함께 확인한다. 회의일을 문서에서 확인할 수 없을 때만 파일명을 보조 근거로 사용하고 그 사실을 보고서에 밝힌다.

과거 검토 보고서의 사실이나 수치를 현재 사실로 재사용하지 않는다. 과거 보고서는 이전 지적, 미해결 과제, 분석 관점과 디자인을 파악하는 데 사용하고, 현재 판단은 최신 Drive 원자료로 다시 검증한다.

## 필수 분석 범위

매 보고서에는 다음 검토가 포함되어야 한다.

- 최신 자료와 직전 자료의 팀 구성을 먼저 대조하고 각 팀의 자료 업데이트 여부를 판정
- 모든 정량 KPI를 최소 `전전 회차 → 직전 회차 → 현재 회차` 3개 시점으로 나란히 비교
- 연속 회차의 전체 수치가 동일하거나 자료 기준일이 갱신되지 않았는지 확인
- 직전 회의록의 요청사항별 `반영 / 일부 반영 / 미반영 / 확인 필요` 판정
- 직전 회의자료 대비 KPI, 계획, 일정, 실행 결과의 변화
- 팀별 보강 내용과 아직 부족한 내용
- 페이지 간 및 자료 간 수치, 합계, 단위, 기간, 연도, 상태 표기의 정합성
- 매출·비용·광고비·공헌이익 등 손익 연결 관계
- 액션별 담당자, 기한, 예상 효과, 경영진 의사결정 필요 여부
- 이전 보고서에서 반복된 지적과 이번 회차에서 새로 발생한 지적의 구분
- 회의 전에 수정할 항목의 우선순위

집계 범위나 정의 차이일 가능성이 있는 수치는 오류로 단정하지 않는다. `집계 범위 확인 필요`처럼 확인 과제로 표현하고, 확정 오류와 분리한다. 자료에서 찾지 못한 항목은 `0`이나 `없음`으로 추정하지 말고 `자료에서 확인되지 않음`으로 적는다.

일부 지표가 우연히 같을 수 있으므로 값 하나가 동일하다는 이유만으로 데이터 미업데이트를 확정하지 않는다. 여러 제품·지표의 값이 직전 회차와 완전히 같거나 기준일·표기일도 과거에 머물러 있으면 `데이터 업데이트 확인 필요`로 판정한다. 담당자 확인이나 명시적인 근거로 미업데이트가 확정되면 `수치 업데이트 안됨`으로 표시하고, 어떤 기간의 값이 반복됐는지 적는다.

과거 검토에서 8월 24일과 9월 2일의 여러 KPI 수치가 완전히 동일한 사실을 찾아 담당자 데이터 미업데이트로 확인한 사례를 검증 기준으로 삼는다. 새 보고서에서도 팀별 전체 지표 세트를 행 단위로 대조해 동일한 반복 패턴을 놓치지 않는다. 다만 이 과거 사례를 현재 회차의 사실로 재사용하지는 않는다.

모든 주요 판단에는 자료명과 슬라이드·페이지 또는 회의록 항목을 근거로 붙인다. 보고서 마지막에는 실제 확인한 자료 목록과 검토 범위, 판단상 제한사항을 적는다.

자료가 제출되지 않은 팀은 이전 내용을 현재 실적으로 간주해 평가하지 않는다. 보고서의 `자료 업데이트 현황`에 `자료 미제출`로 표시하고 Slack 발송용 `latest-report.json`의 `not_submitted_teams` 배열에 기록한다. 자료는 제출됐지만 수치가 갱신되지 않은 팀은 `stale_data_teams` 배열에 기록한다. 그 밖의 부분 업데이트는 `not_updated_teams` 배열을 사용한다.

## 보고서 구조

새 보고서는 원칙적으로 다음 순서를 따른다.

1. 제목과 검토 대상·기준일·비교 자료
2. 핵심 결론
3. 전체 팀 자료 업데이트 현황
4. 업데이트된 각 팀별 검토
   - 주요 변화
   - 전전·직전·현재 수치와 데이터 기준일 비교
   - 직전 회의 요청사항 반영 여부
   - KPI·일정·수치·표기 점검
   - 반복 이슈와 신규 이슈
   - 회의 전 우선 조치와 의사결정 요청
5. 자료가 업데이트되지 않은 팀 목록과 확인 필요사항
6. 팀 간 연결 이슈와 경영진 공통 의사결정 요청
7. 확인 자료와 제한사항

`주요 변화 → 반영 여부 → 점검 → 우선 조치` 흐름을 팀마다 독립적으로 반복한다. 여러 팀의 내용을 하나의 변화표나 반영 여부 표에 섞지 않는다.

자료 성격상 불필요한 빈 섹션은 만들지 않되, 위 분석 항목 자체를 생략해서는 안 된다.

## 파일명과 디자인

- 파일명: `kpi-YYMMDD-review.html`
- `kpi-report-template.html`을 새 보고서의 시작점으로 사용한다.
- 디자인 충돌 시 더 최신인 `kpi-260907-review.html`을 우선 기준으로 삼는다.
- 본문 글꼴은 아래 스택을 그대로 유지한다.
  `-apple-system, BlinkMacSystemFont, "Segoe UI", "Malgun Gothic", "Apple SD Gothic Neo", "Noto Sans KR", Arial, sans-serif`
- 본문, 제목, 설명, 표 머리글과 일반 텍스트는 좌측 정렬한다.
- 숫자, 금액, 비율, 증감값은 `.number` 클래스로 우측 정렬한다.
- 표 셀은 상단 정렬하고, 상태값은 `.status` 클래스로 한 줄 표시한다.
- 본문 최대 폭, 제목 간격, 표 테두리, 회색 머리글, 인쇄 스타일은 템플릿 값을 유지한다.
- 새로운 장식용 색상, 카드형 UI, 중앙 정렬 제목, 별도 웹폰트를 임의로 추가하지 않는다.

## 완료 전 검증

- HTML 문법과 태그 닫힘을 확인한다.
- 데스크톱, 좁은 화면, 인쇄 화면에서 글자와 표가 잘리지 않는지 확인한다.
- 수치 표의 모든 숫자 셀에 우측 정렬이 적용되었는지 확인한다.
- 제목, 본문, 표가 기존 KPI 보고서와 같은 글꼴과 정렬인지 확인한다.
- 원자료에 없는 사실, 담당자, 기한 또는 수치를 추가하지 않았는지 확인한다.
